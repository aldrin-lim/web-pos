const NoSelection = () => {
  return (
    <div className="section flex w-full flex-col items-center justify-center gap-4 text-center">
      <h1 className="text-xl font-bold">Nothing to select</h1>
      <p>
        Seems like all products are already added to the current collection.
      </p>
      <div></div>
    </div>
  )
}

export default NoSelection
